# Web framework for serving your site
flask==3.0.2

# Production server (better than running Flask directly)
gunicorn==22.0.0

# Optional - if you ever handle JSON data for albums/songs
pandas==2.2.2

# Optional - for cross-origin requests if needed
flask-cors==4.0.0
